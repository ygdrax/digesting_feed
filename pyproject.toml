[project]
name = "digesting_feed"
version = "0.1.1"
dependencies = [
    "requests",
    "feedparser",
    "jinja2",
    "beautifulsoup4",
    "sumy",
    "nltk",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "pylint",
    "black",
    "pre-commit",
    "pytest",
    "pytest-mock",
]
